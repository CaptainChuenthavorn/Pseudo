#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
/*int main()
{
	int n, row, column;
	scanf("%d", &n);
	for (column = 1;column <= n; column++)
	{
		for (row = 1; row <= n;row++)
		{
			if (row == 1 || row == n || column == 1 || column == n)
			{
				printf("*");
			}
			else printf(" ");
		}
		printf("\n");
	}
	return 0;
}*/
